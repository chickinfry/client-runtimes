# chik_flutter

`chik_flutter` provides Flutter integrations for Chick-generated application
clients. It builds on the shared `chik_client` runtime.

Generate and use your application's client package with the Chick CLI. The
generated package supplies the application-specific services and models.
