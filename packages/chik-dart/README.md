# chik_client

`chik_client` is the provider-neutral Dart and Flutter runtime used by
Chick-generated application clients.

Generate and use your application's client package with the Chick CLI. The
generated package supplies the application-specific services and models; this
package supplies their shared client runtime.
